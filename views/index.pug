extends layout

block content
  h1= title
  p Welcome to #{title}, created by Jacob Layman
  
  button(type="button", class = "btn btn-secondary", onclick = "goFaster()") Push Me
  div(id = "faster") Increase the speed

  p Link to 
    a(href="/donut") search results
  p Link to
    a(href="/board") board

  script.
    var mph = 20;
    var change = 1;
    function goFaster() {
        mph = mph + change;
        change = change + 1;
        document.getElementById("faster").innerHTML= `MPH ${mph} with change ${change}`
     }